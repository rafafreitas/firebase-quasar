/*
export const someMutation = (state) => {
}
*/

export const hasUpdatedProfile = (state, flag) => {
  state.hasUpdatedProfile = flag
}

export const hasNotifications = (state, flag) => {
  state.hasUpdatedProfile = flag
}
